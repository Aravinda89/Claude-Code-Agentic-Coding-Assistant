import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev overlay badge sits on top of the sidebar footer in this layout.
  devIndicators: false,
};

export default nextConfig;
