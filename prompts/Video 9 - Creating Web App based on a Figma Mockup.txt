npx create-next-app@latest

claude mcp add --transport http figma-dev-mode-mcp-server http://127.0.0.1:3845/mcp
claude mcp add --transport http figma https://mcp.figma.com/mcp

claude mcp add playwright npx @playwright/mcp@latest

Using the following figma mockup (https://www.figma.com/design/QpB) use the figma dev MCP server to analyze the mockup and build the underlying code in this next.js application. Use the recharts library for creating charts to make this a web application. Check how this application looks using the playwright MCP server and verify it looks as close to the mock as possible.

populate these charts with real-world data from FRED